class News {
  dynamic id;
  dynamic url;
  dynamic title;
  dynamic updatedAt;

  News(
      {required this.id,
      required this.url,
      required this.title,
      required this.updatedAt});
}
