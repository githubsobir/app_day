class NewById {
  dynamic content;
  dynamic url;
  dynamic title;
  dynamic updatedAt;

  NewById(
      {required this.content,
        required this.url,
        required this.title,
        required this.updatedAt});
}
