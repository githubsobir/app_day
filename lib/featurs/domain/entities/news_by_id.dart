class NewById {
  dynamic content;
  dynamic url;
  dynamic title;
  dynamic updatedAt;

  NewById(
      {
         this.content,
         this.url,
         this.title,
         this.updatedAt});
}
